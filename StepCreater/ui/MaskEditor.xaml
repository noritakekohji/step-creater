<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="マスクエディタ" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="8">
            <TextBlock VerticalAlignment="Center" Foreground="DimGray"
                       Text="ドラッグで矩形を描画 → [黒塗り] / [枠] / [コメント挿入] → [保存]"/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="8">
            <Button x:Name="BtnBlackout" Content="黒塗り"       Padding="12,4" Margin="0,0,4,0"/>
            <Button x:Name="BtnFrame"    Content="枠"           Padding="12,4" Margin="0,0,4,0"/>
            <Button x:Name="BtnComment"  Content="コメント挿入" Padding="12,4" Margin="0,0,4,0"/>
            <Button x:Name="BtnUndo"     Content="戻す"        Padding="12,4" Margin="0,0,4,0"/>
            <Button x:Name="BtnSave"     Content="保存"        Padding="12,4" Margin="0,0,4,0"/>
            <Button x:Name="BtnCancel"   Content="キャンセル"  Padding="12,4"/>
        </StackPanel>
        <ScrollViewer x:Name="ImageScroller" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="ImageHost" HorizontalAlignment="Left" VerticalAlignment="Top">
                <Image  x:Name="ImgCanvas" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <Canvas x:Name="OverlayCanvas" Background="Transparent" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <Rectangle x:Name="DragRect" Stroke="Red" StrokeThickness="2"
                               Fill="#33FF0000" Visibility="Collapsed"/>
                </Canvas>
            </Grid>
        </ScrollViewer>
    </DockPanel>
</Window>